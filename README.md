# Strands Transformers

Use Transformers as provider in Strands Agents.

# Install

```
git clone git@github.com:cagataycali/strands-transformers.git

python3 -m venv .venv
source .venv/bin/activate

pip3 install -r requirements.txt
```