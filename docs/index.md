<div class="st-hero" markdown>
<div class="st-mesh" aria-hidden="true">
  <span class="st-blob st-blob--1"></span>
  <span class="st-blob st-blob--2"></span>
  <span class="st-blob st-blob--3"></span>
  <span class="st-blob st-blob--4"></span>
</div>
<span class="st-tag">🤗 transformers · 🧬 strands agents</span>
<div class="st-title">Strands Transformers</div>
<p class="st-sub">Run any HuggingFace transformers model from a Strands agent - as a
tool for all&nbsp;24 tasks, or as the agent's own multimodal brain. Local, no API keys.</p>
<div class="st-cta">
  <a class="md-button md-button--primary" href="guide/quickstart/">Quickstart</a>
  <a class="md-button" href="guide/the-tool/">Explore the tool</a>
</div>
</div>

<p align="center" markdown>
  <img src="assets/hero.svg" alt="Every modality in, every modality out - one tool, one local brain, zero hardcoding" style="max-width:880px;width:100%;border-radius:26px;box-shadow:var(--st-glass-shadow);"/>
</p>

```python
# /// script
# dependencies = ["strands-transformers[vision]"]
# ///
import io
from PIL import Image
from strands import Agent
from strands_transformers import TransformerModel

png = io.BytesIO(); Image.new("RGB", (64, 64), (20, 200, 40)).save(png, "PNG")
agent = Agent(model=TransformerModel(model_path="HuggingFaceTB/SmolVLM-256M-Instruct"))
print(agent([{"image": {"format": "png", "source": {"bytes": png.getvalue()}}},
             {"text": "Color? One word."}]))
```

```console
$ uv run hello.py
Green.
```

A 256M vision-language model, wired into the standard agent loop, *seeing* pixels
- in a dozen lines. [Run any snippet in these docs](#run-any-snippet) the same way.

## Why this exists

Wiring HuggingFace into an agent usually means bespoke glue for *every* model -
the right `AutoModel` class, processor, device, dtype, and a custom decode for
each task. It rots the moment transformers ships a new task or you swap models.

strands-transformers deletes that work. It reads transformers' own task taxonomy
**at runtime** ([`SUPPORTED_TASKS`](https://github.com/huggingface/transformers)),
so nothing is hardcoded per task: a new task or model upstream just works here,
no code change. You get **one** way in - through an agent.

## Two entry points

<div class="grid cards" markdown>

-   :material-tools:{ .lg } &nbsp; **`use_transformers`** - the tool

    ---

    One tool exposing every transformers task. **Discover** the taxonomy at
    runtime, **run** a pipeline, or **call** any class/method directly. New model
    or task upstream ⇒ works here with no code change.

    [:octicons-arrow-right-24: The tool](guide/the-tool.md)

-   :material-brain:{ .lg } &nbsp; **`TransformerModel`** - the brain

    ---

    Plug a local HF model in as `Agent(model=…)`. It speaks the agent
    content-block protocol - `image`, `video`, `audio`, `document` reach the
    model directly. VLMs see, audio models hear, Qwen2.5-Omni speaks back.

    [:octicons-arrow-right-24: The agent brain](guide/agent-brain.md)

</div>

## See it - and hear it

Every result below is **real model output**, reproducible from the linked example.

| You send | Model | You get back |
|----------|-------|--------------|
| 🖼️ green image + "Color?" | SmolVLM | `"Green."` |
| 🎬 brightening frames | SmolVLM2-Video | `"BRIGHTER."` |
| 🧰 tool returns a screenshot | SmolVLM | `"Blue."` |
| 📄 a text document | Qwen3 | recovers `BANANA-42` |
| 🔊 a 440 Hz tone | Qwen2.5-Omni | `"It's a pure tone."` |
| 🦾 camera + "pick the cube" | MolmoAct2 | actions `[1,30,6]` |

<div class="result" markdown>
**Hear the library explain itself** - generated by `text-to-audio`, then
re-transcribed by `whisper` to prove it's real speech:

<audio controls src="assets/audio/intro.wav"></audio>
</div>

<div align="center" markdown>
**Real agent outputs** - detection · depth · panoptic segmentation:

<img src="assets/img/gallery.png" width="760"/>

🎬 video understanding &nbsp;·&nbsp; 🔊 generated speech (the library narrating itself):

<img src="assets/video/demo.gif" width="200"/> &nbsp;
<img src="assets/img/waveform.png" width="380"/>
</div>

## Run any snippet

Code blocks here are self-contained [PEP 723](https://peps.python.org/pep-0723/)
scripts - dependencies live in the header. Save one as `demo.py`:

```console
$ uv run demo.py
```

`uv` builds a throwaway env with the right extras and runs it. No `pip install`,
no venv to manage.

## What you can build

<div class="grid cards" markdown>

-   🗣️ **Voice assistant** - speak to it, it speaks back (Qwen2.5-Omni), one local model.
-   🤖 **Robot controller** - camera + instruction → joint actions (MolmoAct, OpenVLA).
-   👁️ **Screen-watcher** - a tool returns a screenshot; the VLM reasons over it.
-   📄 **Document Q&A** - drop a doc block in the conversation, ask about it.
-   🎬 **Video understander** - pass frames, ask what changes over time.
-   🔌 **Any HF task** - ASR, detection, segmentation, embeddings… via one tool.

</div>

## Where next

- New here → **[Installation](guide/installation.md)** · **[Quickstart](guide/quickstart.md)**
- See & hear → **[Content blocks](guide/content-blocks.md)** · **[Audio](guide/audio.md)**
- Robotics → **[Robotics / VLA](guide/robotics.md)**
- Internals → **[Architecture](reference/architecture.md)** · **[API](reference/transformer-model.md)**
